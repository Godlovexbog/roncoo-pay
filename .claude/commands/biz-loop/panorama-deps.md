---
name: "BizLoop: Panorama Deps"
description: 仅提取外部依赖——构建文件 + HTTP 客户端调用 + 中间件
category: biz-loop
tags: [biz-loop, panorama, deps]
---

## /biz-panorama deps

仅运行 Phase 1 的 panorama-dependencies agent。

**执行**: `Skill({skill: "biz-panorama", args: "deps"})`
